name = "ry:sync"
description = "RU: синхронизируй Gemini docs, memories и git. EN: synchronize Gemini docs, memories, and git."
prompt = """
Run rldyour sync for completed Gemini adapter work.

Required behavior:
- Confirm Serena memories are current before durable docs or git synchronization.
- Review uncommitted changes and separate source, docs, generated artifacts, runtime markers, secrets, and local cache files.
- Run scope-matching validators and report exact pass/fail results.
- Commit only coherent source changes with Conventional Commits when mutation is explicitly in scope.
- Never delete branches without explicit owner confirmation.

User args: {{args}}
"""
