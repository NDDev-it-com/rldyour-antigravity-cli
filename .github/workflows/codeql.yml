name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "31 2 * * 1"
  workflow_dispatch:

permissions: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.run_id }}
  cancel-in-progress: false

jobs:
  codeql:
    name: CodeQL (${{ matrix.language }})
    permissions:
      actions: read
      contents: read
      security-events: write
    strategy:
      fail-fast: false
      matrix:
        language: [python, actions]
    uses: NDDev-OpenNetwork/ci-workflows/.github/workflows/public-codeql.yml@36bea6e4ac643872e9a504b2fe19aad9487e398a # 0.2.0-dev
    with:
      languages: '["${{ matrix.language }}"]'
